<Window x:Class="wpf_workspace.LeaderboardWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:wpf_workspace"
        mc:Ignorable="d"
        Title="LeaderboardWindow" Height="283" Width="420" ResizeMode="NoResize">
    <Grid>
        <DataGrid IsReadOnly="True" AutoGenerateColumns="False" Name="PlayerLeaderboard" HorizontalAlignment="Left" Height="235" Margin="10,10,0,0" VerticalAlignment="Top" Width="393">
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding PlayerName}" Header="Имя игрока" Width="192"/>
                <DataGridTextColumn Binding="{Binding PlayerScore}" Header="Счёт" Width="192"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
